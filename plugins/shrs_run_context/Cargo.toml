[package]
name = "shrs_run_context"
version = "0.0.2"
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["MrPicklePinosaur"]
description = "Save and load run context"
repository = "https://github.com/MrPicklePinosaur/shrs"

[dependencies]
shrs = { path = "../../crates/shrs", version = "0.0.2", features = ["serde"] }
clap = { version = "4.1", features = ["derive"] }

ron = { version = "0.8" }
