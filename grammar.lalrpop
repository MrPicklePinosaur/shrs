use std::str::FromStr;
use ast::*;

grammar;

pub PipeSequence: Vec<Command> = {
    <p:PipeSequence> "|" <c:Command> => p.into_iter().chain(c.into_iter()).collect(),
    <c:Command> => vec![c],
}

pub Command: Command = {
    <s:r"[a-z]+"> => Command(s),
}

